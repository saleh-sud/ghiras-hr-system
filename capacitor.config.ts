import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ghiras.hr',
  appName: 'ghiras-al-nahda-hr-system',
  webDir: '.', // تم التعديل هنا: نقطة واحدة فقط بدون مسافات
  bundledWebRuntime: false
};

export default config;