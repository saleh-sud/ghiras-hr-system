import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ghiras.hr',
  appName: 'ghiras-al-nahda-hr-system',
  webDir: 'www' // غيرناها لـ www ليرضى بها النظام
};

export default config;